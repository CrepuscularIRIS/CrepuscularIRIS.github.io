---
title: "跨模态自监督学习：无创脑机接口的语义解码新范式"
description: "MIT/UCL 联合团队通过跨模态自监督学习，将 EEG 和 fMRI 信号对齐到 LLM 语义空间——无需标签，也无需侵入式植入。"
publishDate: "2026-04-12"
tags: ["EEG", "BCI", "Neuroscience", "Foundation Models", "Self-Supervised Learning", "fMRI"]
draft: false
language: zh
hIE: lacia
heroImage:
  src: ../../../assets/hIE/lacia/image.png
---

每一种无创脑机接口系统最终都会撞上同一堵墙：神经信号与语义之间的鸿沟太大了，而现有的标注数据量根本不够用，靠监督学习根本填不上。

EEG 记录中某人正在想象一个句子的信号，在大多数 ML 流程看来就像高维噪声。fMRI 的 BOLD 信号包含丰富的语义信息——早在 Huth et al. (2016) 的研究就表明，词级语义可以从 fMRI 中恢复——但其时间分辨率只有约 1Hz，空间噪声以毫米计，跨session的变异性让同被试监督模型难以规模化，而跨被试泛化则需要对每个新参与者从头训练，数据瓶颈显而易见。

2026年4月初，MIT 与 UCL 的联合团队在 bioRxiv 上发表了一篇论文，以一种我认为真正重要的方式改变了局面：他们不再尝试从神经信号直接映射到词汇，而是通过**跨模态自监督学习**，将 EEG 和 fMRI 信号对齐到预训练视觉和语言模型已有的语义空间中。其结果是得到了一个非侵入式神经信号的基础模型，能够在无需特定语义级别训练数据的情况下实现高保真思维重建。

## 标注数据的核心困境

神经解码的标准做法——尤其是语言和图像相关任务——是这样的：收集大量被试观看或想象有已知标签的刺激物的样本，训练模型从神经信号预测标签，在留出的刺激物上评估。

这在三个相互交织的原因面前崩溃了：

**标注稀缺**：一次典型的 EEG 或 fMRI 扫描，经过预处理剔除噪声段后，大约只能得到 200-500 个有效标注样本。用 400 个样本训练一个 transformer，过拟合几乎是必然的；而生成干净标注的行为范式（受控刺激呈现、按键验证）本身既缓慢又昂贵。

**个体差异**：神经拓扑结构在不同被试之间差异很大——即使是同一个刺激，激活模式看起来也不一样。在被试 A 上训练的模型无法泛化到被试 B，这意味着每个被试都需要足够的数据来训练独立模型，数据收集负担成倍增加。

**语义粒度粗**：标签是粗糙的。你能标注"被试看到了单词'apple'"，但神经信号中包含的内涵、语境和并发心理状态远比标签丰富。一个靠离散标签训练的模型学会了将信号映射到标签类别，而不是信号的实际语义几何结构。

## 跨模态自监督学习

2026年4月这篇论文的核心洞察是：预训练语言和视觉模型已经解决了语义表示问题。CLIP、LLaMA 和类似的模型在高维嵌入空间中编码语义关系，这种空间具有几何意义——相似概念聚集在一起，类比关系形成规则的向量结构，空间能很好地泛化到新输入。

如果你能将神经信号对齐到这个已有的语义几何上，而非对齐到离散标签，你就免费继承了所有这些结构。对齐通过对比自监督学习完成：不需要离散标签，只需要（神经信号，刺激物）配对，让它们在联合嵌入空间中相近。

训练目标是 CLIP 对比损失的跨模态变体：

```python
import torch
import torch.nn as nn
import torch.nn.functional as F
from typing import Optional

class CrossModalContrastiveLoss(nn.Module):
    """
    Cross-modal contrastive loss aligning neural signals to 
    pre-trained language/vision model embeddings.
    
    Positive pairs: (neural_signal_i, stimulus_embedding_i) for same trial
    Negative pairs: (neural_signal_i, stimulus_embedding_j) for j != i
    """
    
    def __init__(self, temperature: float = 0.07):
        super().__init__()
        self.temperature = temperature
    
    def forward(
        self,
        neural_embeddings: torch.Tensor,  # (batch, embed_dim)
        stimulus_embeddings: torch.Tensor,  # (batch, embed_dim)
        mask: Optional[torch.Tensor] = None  # Mask for invalid pairs
    ) -> torch.Tensor:
        # L2 normalize both embedding spaces
        neural_norm = F.normalize(neural_embeddings, dim=-1)
        stimulus_norm = F.normalize(stimulus_embeddings, dim=-1)
        
        # Similarity matrix: (batch, batch)
        similarity = torch.matmul(neural_norm, stimulus_norm.T) / self.temperature
        
        if mask is not None:
            # Mask invalid pairs (e.g., trials from same class that should not be negatives)
            similarity = similarity.masked_fill(mask, float('-inf'))
        
        # Symmetric contrastive loss
        labels = torch.arange(similarity.size(0), device=similarity.device)
        loss_n2s = F.cross_entropy(similarity, labels)  # neural → stimulus
        loss_s2n = F.cross_entropy(similarity.T, labels)  # stimulus → neural
        
        return (loss_n2s + loss_s2n) / 2.0


class NeuralSignalEncoder(nn.Module):
    """
    Transformer-based encoder for EEG/fMRI signals.
    Maps raw neural signals to the shared semantic embedding space.
    """
    
    def __init__(
        self,
        signal_dim: int,          # EEG: num_channels; fMRI: num_ROIs or voxels
        seq_len: int,             # Temporal dimension
        embed_dim: int = 768,     # Match target model (e.g., CLIP-ViT-B)
        n_heads: int = 12,
        n_layers: int = 8,
        dropout: float = 0.1,
        modality: str = "eeg"     # "eeg" or "fmri"
    ):
        super().__init__()
        self.modality = modality
        
        # Modality-specific preprocessing
        if modality == "eeg":
            # Treat EEG channels as "patches" — temporal embedding per channel
            self.patch_embed = nn.Conv1d(signal_dim, embed_dim, kernel_size=8, stride=4)
        elif modality == "fmri":
            # Project ROI activations to embedding space
            self.patch_embed = nn.Linear(signal_dim, embed_dim)
        
        # Learnable positional embeddings
        # EEG: Conv1d with stride=4 reduces seq_len to seq_len//4; fMRI: no downsampling
        pos_seq_len = (seq_len // 4 + 1) if modality == "eeg" else (seq_len + 1)
        self.pos_embed = nn.Parameter(torch.randn(1, pos_seq_len, embed_dim) * 0.02)
        self.cls_token = nn.Parameter(torch.randn(1, 1, embed_dim) * 0.02)
        
        # Transformer backbone
        encoder_layer = nn.TransformerEncoderLayer(
            d_model=embed_dim,
            nhead=n_heads,
            dim_feedforward=embed_dim * 4,
            dropout=dropout,
            batch_first=True,
            norm_first=True  # Pre-norm for stability with small batches
        )
        self.transformer = nn.TransformerEncoder(encoder_layer, num_layers=n_layers)
        self.norm = nn.LayerNorm(embed_dim)
        
        # Projection head for contrastive alignment
        self.proj = nn.Sequential(
            nn.Linear(embed_dim, embed_dim),
            nn.GELU(),
            nn.Linear(embed_dim, embed_dim)
        )
    
    def forward(self, x: torch.Tensor) -> torch.Tensor:
        """
        Args:
            x: Neural signal tensor
               EEG: (batch, channels, time_points)
               fMRI: (batch, time_points, ROIs)
        Returns:
            embeddings: (batch, embed_dim) — CLS token representation
        """
        batch_size = x.shape[0]
        
        if self.modality == "eeg":
            # Convolve along time axis: (batch, embed_dim, seq_len//4)
            x = self.patch_embed(x).permute(0, 2, 1)
        elif self.modality == "fmri":
            x = self.patch_embed(x)  # (batch, seq_len, embed_dim)
        
        # Prepend CLS token
        cls_tokens = self.cls_token.expand(batch_size, -1, -1)
        x = torch.cat([cls_tokens, x], dim=1)
        
        # Add positional embeddings
        x = x + self.pos_embed[:, :x.shape[1], :]
        
        # Transformer forward
        x = self.transformer(x)
        x = self.norm(x)
        
        # CLS token as sequence representation
        cls_output = x[:, 0, :]
        
        # Project to contrastive space
        return self.proj(cls_output)
```

训练时，神经编码器被训练生成与预训练模型（视觉刺激用 CLIP，语言刺激用 LLaMA）的冻结嵌入对齐的表示。预训练模型本身不更新——只有神经编码器在学习。这很关键，因为它意味着神经编码器是被拉入一个具有已知几何属性的语义空间，而不是在构建一个新的任意空间。

## 自监督信号的设计

使得这一方法在不需要海量标注数据集的情况下变得可行的关键设计选择，是如何构建正样本对。

**跨 session 对齐**：对于给定被试，同一刺激物的不同 session 记录可以提供正样本对，而不需要*语义类别标签*——只需要刺激物身份元数据（呈现的是哪张图或哪个句子）来配对样本。模型学习到：这些不同的神经记录——尽管有噪声和 session 间变异性——应该映射到语义嵌入空间中的相近位置，因为它们对应同一个刺激物。

**跨被试锚定**：预训练模型的嵌入对所有被试都是相同的（同一刺激物 → 同一 CLIP 嵌入，无论谁来观看）。这创造了自动的跨被试对齐信号：两个不同被试对同一图像的神经反应应该映射到 CLIP 空间中的同一点。模型因此作为训练目标的副产品学会了被试不变表示，无需任何显式的被试归一化步骤。

**时序结构**：对于语言刺激，BERT 风格的词嵌入时序排序提供了天然的学习课程。模型不仅学习预测正在处理哪个概念，还学习句子展开过程中语义内容的时序演变。

```python
class CrossModalNeuralDecoder:
    """
    Decoding interface using a trained NeuralSignalEncoder.
    Recovers semantic content from neural signals via nearest-neighbor
    search in the pre-trained model's embedding space.
    """
    
    def __init__(
        self,
        encoder: NeuralSignalEncoder,
        stimulus_library: dict[str, torch.Tensor],  # {label: embedding}
        top_k: int = 5
    ):
        self.encoder = encoder
        # Pre-compute stimulus embedding matrix
        labels = list(stimulus_library.keys())
        embeddings = torch.stack(list(stimulus_library.values()))
        self.labels = labels
        self.stimulus_embeddings = F.normalize(embeddings, dim=-1)
        self.top_k = top_k
    
    @torch.inference_mode()
    def decode(self, neural_signal: torch.Tensor) -> list[tuple[str, float]]:
        """
        Decode a neural signal to nearest stimuli in semantic space.
        
        Returns: List of (label, similarity_score) sorted by score descending.
        """
        neural_embedding = self.encoder(neural_signal.unsqueeze(0))
        neural_embedding = F.normalize(neural_embedding, dim=-1)
        
        similarities = torch.matmul(neural_embedding, self.stimulus_embeddings.T).squeeze()
        top_k_scores, top_k_indices = torch.topk(similarities, k=self.top_k)
        
        return [
            (self.labels[idx.item()], score.item())
            for idx, score in zip(top_k_indices, top_k_scores)
        ]
```

## 2026年4月论文的结果

论文在三个基准任务上报告了结果：图像重建（从 fMRI 预测被试看到的图像）、词汇识别（从 EEG 识别静默阅读中的单词）、语义类别解码（从两种模态中识别更广的类别）。

关键数字：

- **图像重建**（fMRI，24名被试，1000张图像评估）：从1000个候选中正确识别图像的 top-1 准确率为 67.3%，对比此前监督学习 SOTA 的 43.2%。24个百分点的差距——大到足以代表质的飞跃，而非增量提升。

- **跨被试泛化**：在 N-1 个被试上训练、在留出被试上测试的模型达到了被试内性能的 89%，对比之前方法的约 61%。语义对齐目标隐式地学习了被试不变表示。

- **数据效率**：该方法达到与监督基线同等性能所需的标注数据量减少了约 40 倍。大部分学习来自无标注的跨 session 配对，而非标注样本。

- **零样本迁移到新刺激物**：这是我发现最有意义的结果。因为模型映射到 CLIP/LLaMA 空间，它可以解码从未出现在训练集中的刺激物的神经反应——只需在预存嵌入库中找到最近邻。之前的任何方法都无法泛化到训练刺激物之外。

## 依然存在的现实鸿沟

结果令人印象深刻，但对局限性的诚实盘点同样重要：

**EEG 的时间 vs. 空间分辨率**：该方法对 fMRI（高空间分辨率、低时间分辨率）的效果优于 EEG（低空间分辨率、高时间分辨率），在视觉解码任务上尤其如此。EEG 的优势在于时间——它能捕捉到 fMRI 无法做到的语言处理的毫秒级动态。文中描述的跨模态 SSL 目标尚未充分利用这一点。

**个体差异被减少，但未消除**：89% 的跨被试性能听起来不错，但剩余的 11% 差距并非随机分布。它集中在神经拓扑结构不典型的被试中——而这些人恰恰是脑机接口技术临床意义最大的神经疾病患者。

**CLIP 对齐假设**：假设 CLIP 或 LLaMA 的语义空间能正确捕捉神经信号的结构，是一个强先验。有越来越多的证据表明语言模型的语义空间与人类神经语义空间共享几何结构——但它们并不完全相同。对齐到 LLM 空间可能是在施加一种表征结构，扭曲了 LLM 训练分布中没有对应物的某些神经区分。

**解码 ≠ 理解**：恢复某人观看了哪个图像类别并不意味着我们理解了大脑如何表征图像。该方法是强大的解码工具，但对神经机制保持不可知——如果你的目标是神经科学而非 BCI，这就很重要了。

## 为什么这对整个领域重要

意义不仅仅在于基准数字，而在于数据收集问题变成了什么样子。

之前的无创 BCI 范式要求在受控实验 session 中，被试观看或想象一组特定的标注刺激物。你无法为任意心理内容构建解码器，因为你无法收集任意心理内容的标注样本。系统从根本上受限于可能思维的组合爆炸和标注它们的不可能性。

跨模态 SSL 打破了这个瓶颈。如果你的神经编码器能将信号映射到一个已经捕捉了任意语义内容（因为它对齐到了一个理解任意语义内容的模型）的空间中，那么每一条神经记录都潜在可解码，而不仅仅是预设刺激物的记录。

对于无创 BCI 应用——通讯辅助、心理意象工作空间、认知负荷监测——这是向"无需针对你最终想要解码的特定词汇或图像进行训练"目标迈出的有意义一步。

通往那个目标的距离仍然不小。但达到目标的架构路线现在比六个月前清晰多了。

---

*来源："Cross-Modal Self-Supervised Foundation Models for Non-Invasive Neural Decoding"（bioRxiv，2026年4月8日，MIT/UCL）；Huth et al.，"Natural speech reveals the semantic maps that tile human cerebral cortex，" Nature 2016；CLIP（Radford et al.，OpenAI 2021）；Scotti et al.，"MindEye2: Shared-Subject Models Enable fMRI-To-Image With 1 Hour of Data，" ICML 2024。*
